# in genaeral we see the range as 
number = range(1,10,1)      # range(start,stop,step)
print(number)

# but in the sceanrio of the lists it stores the data like
number = tuple(range(1,10,1))    # range(start,stop,step)
print(number)