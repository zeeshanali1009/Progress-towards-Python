# parameters are the variables or the containers which we use during the function definition
# arguments are the variables or the containers which we use during the calling for the function

def Multiply_numbers(num_1, num_2):
    number = num_1 * num_2
    return number

result = Multiply_numbers(12,12)
print(f"The multiplcation of the two numbers is : {result}")