# we will be discussing the role of lambda functions as they are very much important because lamda makes the
# function definition very much concise and precsise 

# as it has been very much cleared that lambda use has made the function definition very much easier and 
# convenient
square =lambda x:x*x
print (square(3))

sum =lambda a,b,c:a+ b +c
print(sum(3,4,5))
